springdoc:
  swagger-ui:
    enabled: false

logging:
  level:
    root: INFO
    org.hibernate.SQL: WARN
    org.hibernate.type.descriptor.sql.BasicBinder: WARN
    org.springframework.data.jpa.repository.query: WARN
    org.springframework.transaction: WARN
    com.gustoexpedition: INFO
  file:
    name: ${LOG_PATH:-/var/log/gusto}/gusto-backend.log

spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: false
        show_sql: false
    open-in-view: false

LOG_FORMAT: ${LOG_FORMAT:PLAIN}
LOG_PATH: ${LOG_PATH:-/var/log/gusto}

gusto:
  cors:
    # 프로덕션 환경: 프로덕션 origin만 허용 (환경 변수로 설정)
    allowed-origins:
      - ${CORS_ALLOWED_ORIGIN:}
      - ${CORS_ALLOWED_ORIGIN_2:}
    allow-credentials: true
