services:
  db:
    image: postgres:16
    container_name: gusto-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: gusto
      POSTGRES_USER: gusto
      POSTGRES_PASSWORD: gusto
    volumes:
      - gusto_pgdata:/var/lib/postgresql/data

volumes:
  gusto_pgdata:
